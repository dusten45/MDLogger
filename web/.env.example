# Supabase hosted project 설정 (spec §5.1).
# 실제 값은 .env.development / .env.preview / .env.production에 넣는다.
# publishable(anon) key만 사용한다. service-role key는 절대 넣지 않는다.

VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_...
